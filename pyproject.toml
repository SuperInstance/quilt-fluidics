[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-fluidics"
version = "0.1.0"
description = "The Coupling Charter compiled: Ferre/Filter/State primitives, Reynolds-Number rider, Coupled Lorenz attractors, 3-4-5 jig precedence."
authors = [{ name = "Casey", email = "casey@superinstance.dev" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
